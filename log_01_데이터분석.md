# 실험 로그 01 — 데이터 분석: 텍스트만으로 57.5%

- 날짜: 2026-08-28
- 대회: SSAFY 16기 1회차 AI 챌린지 (재활용품 이미지 VQA)
- 목적: 모델을 건드리기 전에 데이터의 성질과 **진짜 기준선**을 확정한다
- 상태: 완료. 이후 모든 실험의 판정 기준이 됨

---

## 1. 데이터 형태

| | 행 수 | 컬럼 |
|---|---|---|
| `train.csv` | 5,073 | id, path, question, a, b, c, d, answer |
| `dev.csv` | 4,413 | 위 + answer1~answer5 |
| `test.csv` | 5,074 | answer 없음 |

- 4지선다, 정답은 `a`/`b`/`c`/`d` 한 글자. 평가 지표는 accuracy
- **이미지 1장당 문항 1개** (중복 이미지 없음) → 이미지 단위 split == 행 단위 split
- 이미지 원본 해상도 중앙값 **720×960**, train/test 각 약 619MB
- 질문 평균 길이 **32자**, 고유 질문 2,303개 / 5,073행

---

## 2. 핵심 수치 — 텍스트 전용 기준선

이미지를 **전혀 주지 않고** 질문+선택지 텍스트만으로 분류기를 학습시켰다.

```
특징  : 문자 n-gram TF-IDF (analyzer="char_wb", ngram_range=(2,4), min_df=2)
모델  : LogisticRegression(C=2.0, max_iter=2000)
구성  : (문항 × 선택지 4개) 이진 분류 → 문항별 argmax
학습  : fit 4,566문항 / 검증 val 299문항
```

| | 정확도 | 95% CI |
|---|---|---|
| 랜덤 | 25.0% | — |
| **텍스트 전용** | **57.5%** | 51.9 – 63.0% |

> **판정 기준 확정**: VLM이 57.5%를 못 넘으면 이미지를 보고 있지 않다는 뜻.
> 이후 모든 모델은 이 선을 기준으로 판정한다.

⚠️ **표본 관련 정정 (2026-08-28 추가)** — 이 57.5%는 **299장 부분집합** 기준이다.
이후 해상도 실험에서 val 507장 전체로 재보니, 이 299장이 나머지 208장보다
**8.4%p 쉬운 표본**이었음이 드러났다 (384px Qwen3 기준 부분집합 86.3% vs 나머지 77.9%, 2.4 SD).
층화추출 자체는 정상 동작했고(질문 유형 비율 일치) 우연히 쉬운 draw를 뽑은 것으로 보인다.

**이후 모든 실험은 val 507장 전체를 기준으로 한다.** 299장 값은 과거 실험과의 연결용으로만 병기.
외부(타 참가자)와 절대값을 비교할 때는 반드시 표본 크기를 맞출 것.

---

## 3. 데이터에 박힌 편향 (텍스트 전용이 높은 이유)

| 편향 | 수치 |
|---|---|
| 정답 위치(a/b/c/d) 분포 | 24~27% — **균등, 위치 트릭 없음** |
| 가장 긴 선택지를 찍기 | **42.4%** 적중 |
| 재질 문제 정답 중 플라스틱 계열 | 약 28% |
| 개수 문제 정답 숫자가 "2개" | **44%** |
| 개수 문제 정답이 4개 선택지 중 두 번째로 작은 값 | 50% |

재질 문제의 선택지 자체는 균등하게 등장한다(플라스틱 635 / 유리 629 / 금속 613 / 종이 610). **편향은 선택지 등장 빈도가 아니라 정답 쪽에 있다.**

---

## 4. 질문 유형 분포

`qtype` 분류 규칙: `"몇" in q → 개수`, `"재질" in q → 재질`, `"색" in q → 색상`, 나머지 기타

| 유형 | train | test | dev |
|---|---|---|---|
| 개수 | 34.6% | 33.9% | **71.5%** |
| 재질 | 31.8% | 30.1% | 13.0% |
| 기타 (물체 식별) | 21.8% | 24.1% | 8.7% |
| 색상 | 7.3% | 7.2% | 3.0% |

> "기타"는 대부분 `"...는 무엇인가요?"` 형태의 **물체 식별** 문항이다.
> 예: *"사진에 보이는 재활용품은 무엇인가요?"*, *"사진 속 손에 들린 재활용품은 어떤 종류의 용기인가요?"*

---

## 5. `dev.csv`를 검증셋으로 쓰면 안 된다

두 가지 이유가 각각 독립적으로 치명적이다.

**(1) 분포 불일치** — 개수 문제가 dev 71.5% vs test 33.9%. 개수는 모든 모델이 가장 못 푸는 유형이라(4절 참고), dev로 튜닝하면 test에서 어긋난다.

**(2) 라벨 노이즈** — `answer1`~`answer5`의 일치도:

| 다수결 득표수 | 건수 | 비율 |
|---|---|---|
| 5/5 (만장일치) | **1** | 0.02% |
| 4/5 | 902 | 20.4% |
| 3/5 | 2,606 | 59.1% |
| 2/5 | 903 | 20.5% |
| 1/5 | 1 | 0.02% |

4,413건 중 만장일치가 **1건**. 개별 답안이 다수결과 일치하는 비율은 평균 **60%**.

참고로 균등 랜덤 5회 추출이라면 max=2가 58.6%, max=3이 35.2%다. 관측치(max=2가 20.5%, max=3이 59.1%)는 랜덤보다는 집중돼 있지만 사람 라벨이라 보기엔 지나치게 흩어져 있다.

**결론**: 검증은 `train`에서 직접 분리한다. `SEED=42`, `VAL_FRAC=0.10` → val_full 507장, 질문 유형 층화추출 부분집합 299장.

---

## 6. train ↔ test 중복

| | 비율 |
|---|---|
| test 질문 문자열이 train에 등장 | 64.4% |
| test의 (질문 + 선택지 집합)이 train에 그대로 존재 | 33.8% |
| ↳ 그중 train 내부에서 정답이 하나로 고정되는 비율 | **53%** |

전체 고유 질문: train 2,303 / test 2,286, 교집합 613개.

**해석**: 언어 패턴은 크게 겹치므로 파인튜닝 효과가 잘 날 조건이다. 동시에 같은 질문+선택지라도 **정답이 절반은 갈린다** → 나머지는 이미지를 봐야 풀린다. 텍스트 프라이어의 천장이 약 58%라는 것과 일관된다.

---

## 7. 결론 → 이후 실험 설계에 반영한 것

1. **판정선 57.5%** — 모든 VLM은 이 값 대비로 읽는다
2. **dev.csv 미사용**, train 자체 split (`SEED=42`, `VAL_FRAC=0.10`)
3. **층화추출** — 질문 유형 비율을 test에 맞춤 (개수 33%, 재질 32%, 기타 28%, 색상 7%)
4. **로짓 비교 추론** — 정답이 한 글자이므로 `generate()` 대신 forward 1회
   (→ 실험 로그 03에서 이 선택의 실측 효과를 따로 검증. **결론: 정확도 이득은 없었다**)
5. **검증 표본은 507장 전체** — 위 정정 참고

---

## 부록 · 재현

```bash
python kanana_bakeoff.py --dry-run     # split + 텍스트 기준선만 재계산
```

또는 `step1_2_bakeoff.ipynb`의 2~3번 셀. 텍스트 전용 확률은
`bakeoff_out/text_only_probs.npy`에 (299, 4) 형태로 저장된다.

```python
# split 재현 (모든 실험이 공유)
rng  = np.random.RandomState(42)
perm = rng.permutation(len(train_df))
k    = int(len(train_df) * 0.10)
val_full = train_df.iloc[perm[:k]].reset_index(drop=True)   # 507
# 질문 유형 층화추출 → 299
```
