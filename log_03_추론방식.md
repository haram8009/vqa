# 실험 로그 03 — 추론 방식: 가설이 두 번 틀린 기록

- 날짜: 2026-08-28
- 목적: 베이스라인의 `generate()` + 파싱 대비 로짓 비교가 실제로 얼마나 이득인지 측정
- 상태: 완료. **결론은 음성(null) — 정확도 이득 없음**
- 스크립트: `infer_method_compare.ipynb`

---

## 1. 배경 — 두 가지 가설

지금까지의 점수는 전부 **a/b/c/d 로짓 비교**로 잰 값이고, 배포 베이스라인은 **`generate()` + 문자열 파싱**을 쓴다. 두 가설을 세웠다.

**가설 A** — 베이스라인의 디코딩에 버그가 있고, 그게 점수를 깎고 있다.

```python
out_ids = model.generate(**inputs, max_new_tokens=2, do_sample=False, ...)
output_text = processor.batch_decode(out_ids, skip_special_tokens=True)[0]   # ← 전체 시퀀스
preds.append(extract_choice(output_text))
```

`out_ids`에는 입력 프롬프트가 그대로 들어 있는데 통째로 디코딩한다. 파서가 보는 "마지막 줄"이 모델의 답이 아닐 수 있고, 파싱에 실패하면 **무조건 `"a"`** 로 찍는다.

**가설 B** — 로짓 비교가 파싱을 거치지 않으므로 더 정확하다.

---

## 2. 설계 — 2 모델 × 3 방식

가설 A와 B를 분리하려면 중간 조건이 필요하다. ①과 ③만 비교하면 *"generate가 나쁘다"*와 *"베이스라인의 디코딩이 잘못됐다"*를 구분할 수 없다.

| | ① generate + 전체 디코딩 | ② generate + 새 토큰만 | ③ 로짓 비교 |
|---|---|---|---|
| Qwen2.5-VL-3B | **A** ← 배포 베이스라인 원본 | **A′** | **B** |
| Qwen3-VL-4B | **C** | **C′** | **D** |

②는 프롬프트를 잘라내고 디코딩한다.

```python
gen_ids = out_ids[:, inputs["input_ids"].shape[1]:]
output_text = processor.batch_decode(gen_ids, skip_special_tokens=True)[0]
```

조건: val 299장 · 384px · 같은 프롬프트 · 같은 이미지 로딩 · 학습 없음 · `max_new_tokens=2`, `do_sample=False` (베이스라인 원본 값). 파서는 베이스라인 `extract_choice` 코드를 그대로 복제하고, "파싱에 성공했는지"만 함께 기록하는 계측 버전을 추가했다.

---

## 3. 결과

| | ① generate 전체디코딩 | ② generate 새토큰만 | ③ 로짓 비교 |
|---|---|---|---|
| Qwen2.5-VL-3B | 77.59% | 77.59% | 77.26% |
| Qwen3-VL-4B | 86.29% | 86.29% | 86.29% |

**세 방식이 거의 완전히 같다.**

### 예측이 실제로 동일한가

| 비교 | 서로 다른 문항 |
|---|---|
| Qwen2.5 · ① vs ② | 0 / 299 |
| Qwen2.5 · ① vs ③ | 3 / 299 |
| Qwen3 · ① vs ② | 0 / 299 |
| Qwen3 · ① vs ③ | **0 / 299** |

### 파싱 실패

**네 조건 모두 0건.** 가설 A가 예측한 실패가 한 번도 나오지 않았다.

---

## 4. 왜 같은가 — 두 이유는 성격이 다르다

### ① = ② : 우연히 무해했다 (가설 A 기각)

전체 디코딩 원문의 마지막 80자를 찍어보면:

```
'속\n(b) 유리\n(c) 플라스틱\n(d) 종이\n\n정답을 반드시 a, b, c, d 중 하나의 소문자 한 글자로만 출력하세요.\nassistant\nc'
```

채팅 템플릿이 생성된 글자를 `assistant` 다음 **독립된 줄**에 놓는다. `extract_choice`는 마지막 비어있지 않은 줄을 보므로 정확히 `"c"`를 얻는다. 새 토큰만 디코딩한 ②의 결과는 `'c'` — **파서 입장에서 둘은 같은 입력이다.**

버그는 실재한다(프롬프트를 디코딩하는 건 맞다). 다만 **이 프롬프트·템플릿 조합에서는 결과에 영향이 없었다.**

### ② ≈ ③ : 수학적으로 거의 같은 연산이다 (가설 B 기각)

이건 미리 예상했어야 했다.

- `do_sample=False` = **greedy decoding** = 첫 토큰이 **전체 vocab에 대한 argmax**
- 로짓 비교 = **{a,b,c,d}로 제한한 argmax**

모델의 전역 1위 토큰이 이미 a/b/c/d라면 **두 방식은 정의상 동일한 답을 낸다.** Qwen3는 299번 모두 그랬다. Qwen2.5는 3번 벗어났다.

갈린 3문항을 열어보면 전부 확률이 거의 평평하다:

| # | gold | generate | logit | 로짓 확률 |
|---|---|---|---|---|
| 57 | b | a | c | a .270 / b .238 / **c .306** / d .186 |
| 210 | c | **c** | b | a .197 / **b .325** / **c .325** / d .153 |
| 214 | c | a | d | **a .357** / b .070 / c .169 / **d .404** |

모델이 가장 헷갈려한 문항들이다. 3:1로 generate가 이겼지만 표본 3개라 노이즈다.

---

## 5. 개선폭 분해

| 단계 | 값 | 기여 |
|---|---|---|
| A — Qwen2.5 + generate 전체디코딩 (배포 원본) | 77.59% | — |
| + 디코딩 버그 수정 | 77.59% | **0.00%p** |
| + 로짓 방식 전환 | 77.26% | **−0.33%p** |
| + 모델 교체 | 86.29% | **+9.03%p** |
| D — Qwen3 + 로짓 (최종) | 86.29% | |
| **총 개선폭** | | **+8.70%p** |

**배포 베이스라인 대비 개선의 사실상 전부가 모델 교체 효과다.**

McNemar(같은 모델, 방식만 변경)는 네 쌍 모두 p > 0.4로 무의미.

---

## 6. 그럼 로짓 비교를 왜 계속 쓰는가

정확도 때문은 아니다. 남은 이유는 두 가지다.

**속도** — 0.114 s/장 vs 0.207 s/장. **1.8배 빠르다.** test 5,074장에서 10분 vs 18분. 해상도를 640으로 올려도 19분에 끝나는 여유가 여기서 나온다.

**확률 부산물** — 앙상블 검증, 위치 편향 보정 실험, 모델 간 상보성 분석이 전부 저장된 확률값으로 **추가 GPU 실행 없이** 가능했다. `generate()`는 문자 하나만 남긴다.

그리고 **파싱 성공은 운이었다.** 템플릿이 마침 글자를 독립된 줄에 놓아준 덕이다. 프롬프트 끝 형태를 바꾸거나 다른 모델로 갈면 깨질 수 있다. 5,074장 제출에서 이 우연에 기대는 것보다는 구조적으로 실패가 불가능한 쪽이 낫다.

---

## 7. 배운 것

**틀린 가설 두 개를 실측으로 확인했다.**

- 가설 A(디코딩 버그가 점수를 깎는다) → 실제 영향 0.00%p
- 가설 B(로짓 비교가 더 정확하다) → 실제 −0.33%p

*"코드에 버그가 보인다"*와 *"그 버그가 점수에 영향을 준다"*는 별개다. 재는 데 25분 걸렸고, 안 쟀으면 **모델 교체 효과 +9.0%p의 일부를 추론 방식 덕으로 잘못 귀속할 뻔했다.**

그리고 `do_sample=False`가 argmax라는 사실을 미리 짚었다면 ②≈③은 실험 전에 예측할 수 있었다. **설계 단계에서 "이 두 방식이 수학적으로 다른가"를 먼저 물었어야 했다.**

---

## 부록 · 재현

```bash
jupyter notebook infer_method_compare.ipynb
```

결과는 `bakeoff_out/infer_<모델>_<방식>.json`에 조건별로 저장된다(재실행 시 자동 재사용).
요약은 `infer_method_summary.json`, 그림은 `infer_method.png`.

```python
# 베이스라인 파서 (원본 그대로)
def extract_choice(text):
    lines = [l.strip() for l in text.strip().lower().splitlines() if l.strip()]
    if not lines: return "a"
    last = lines[-1]
    if last in ["a","b","c","d"]: return last
    for tok in last.split():
        if tok in ["a","b","c","d"]: return tok
    return "a"                      # 파싱 실패 시 무조건 "a"
```

> ⚠️ 이 로그의 숫자는 **299장 부분집합** 기준이다. 이후 507장 전체로 통일했다(로그 01 참고).
> 다만 이 실험은 **같은 표본 안에서 방식만 비교**하므로 결론은 그대로 유효하다.
