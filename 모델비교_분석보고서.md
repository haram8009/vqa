# VQA 모델 비교 분석 보고서

**SSAFY 16기 1회차 AI 챌린지 — 재활용품 이미지 VQA**
측정일 2026-08-28 · zero-shot(학습 없음) · val 299장

---

## 1. 결론

**`Qwen/Qwen3-VL-4B-Instruct`를 채택한다.** 정확도 86.3%로 1위이면서 동시에 가장 빠르다(추론 10분, kanana의 3배). 베이스라인이 쓰던 Qwen2.5-VL-3B를 이 모델로 **교체하는 것만으로 +9.0%p**를 얻는다 — 추론 방식은 고정하고 모델만 바꾼 값이다(2.3절 참조).

부수적으로 확인된 세 가지가 이후 계획을 바꾼다.

| 발견 | 계획에 미치는 영향 |
|---|---|
| 앙상블·텍스트 블렌딩이 **단일 모델보다 못하다** | 이 레버는 버린다 |
| Qwen3의 유일한 약점이 **개수 세기(74%)** — 나머지는 92~95% | 해상도가 유일하게 남은 큰 레버 |
| 한국어 특화(kanana)의 이점이 **측정되지 않았다** | 한국어 특화 모델 탐색에 시간을 쓸 이유 없음 |

---

## 2. 실험 설계

### 2.1 조건

| 항목 | 값 |
|---|---|
| 검증 세트 | train에서 분리한 자체 split 299장 (`SEED=42`, `VAL_FRAC=0.10`, 질문 유형 층화추출) |
| 이미지 해상도 | 384×384 (베이스라인과 동일) |
| dtype | bfloat16 (RTX 5060 Ti, compute capability 12.0) |
| 양자화 | 없음 |
| 학습 | **없음 (zero-shot)** |
| 추론 방식 | a/b/c/d 4개 토큰의 로짓 비교 (forward 1회) |
| 프롬프트 | 베이스라인과 동일 — 모델 비교에서 프롬프트는 변수로 두지 않음 |

### 2.2 설계상의 선택과 이유

**`dev.csv`를 검증에 쓰지 않았다.** dev는 개수 문제 비중이 71.5%인데 test는 33.9%로 분포가 완전히 다르다. 또 5개 답안 컬럼의 만장일치가 4,413건 중 1건뿐이라 라벨 자체가 매우 노이즈하다. 이 셋으로 튜닝하면 test에서 무너진다. 검증은 train에서 직접 떼어냈고, 질문 유형 비율을 test에 맞춰 층화추출했다.

**생성 대신 로짓 비교를 썼다.** 정답이 한 글자이므로 `generate()`는 낭비다. 다음 토큰 분포에서 a/b/c/d 네 개만 꺼내 비교하면 **파싱 실패가 0**이 되고 2~3배 빨라지며, 확률값이 남아 사후 분석(앙상블·보정)이 가능해진다. 이 보고서의 분석 대부분이 그 확률값 덕분에 가능했다.

**표본 299장의 한계를 명시한다.** 정확도 86% 근처에서 95% 신뢰구간의 폭이 약 ±4%p다. 아래 결과의 순위는 통계 검정으로 뒷받침했지만, 4%p 이내의 차이는 이 표본으로 단정할 수 없다.

### 2.3 이 숫자들이 무엇의 비교인지 — 읽는 법

**세 모델 모두 동일한 로짓 비교 방식으로 측정했다.** 추론 방식을 고정하고 모델만 바꿨으므로 **모델 간 비교는 공정하다.** 3절의 순위와 5·6절의 분석은 모두 이 전제 위에 있다.

**단, 배포된 베이스라인 노트북의 점수와는 비교한 적이 없다.** 배포 베이스라인은 `generate()` + 문자열 파싱이고 학습 데이터도 `sample(n=200)`으로 제한한다. 이 조합은 본 실험에서 한 번도 측정하지 않았다. 따라서 1절의 *"베이스라인이 쓰던 Qwen2.5-VL-3B를 교체하는 것만으로 +9.0%p"*는 다음을 뜻한다.

> 로짓 비교 방식을 고정한 상태에서 **모델만** Qwen2.5-VL-3B → Qwen3-VL-4B로 바꾼 효과 = **+9.0%p**

배포 베이스라인을 그대로 돌렸을 때 대비 총 개선폭은 **아직 측정되지 않았다.** 그 값을 알려면 `generate()` 방식으로 같은 val 299장을 한 번 더 재야 한다(약 10분). 추론 방식 자체의 기여분은 미측정 항목으로 남긴다.

**추론 방식 변경이 사는 것**

| | `generate()` + 파싱 (베이스라인) | a/b/c/d 로짓 비교 (본 실험) |
|---|---|---|
| 파싱 실패 | 발생 가능 — `"정답은 (c)입니다"`, 빈 출력 등 | **구조적으로 0** |
| 속도 | 기준 | 2~3배 빠름 |
| 부산물 | 문자 하나 | **확률 4개** → 앙상블·보정·상보성 사후 분석 가능 |

**대회 규정 적합성.** 로컬에 로드한 모델의 forward 1회일 뿐이며 외부 API 호출도, test 라벨 접근도 없다. 제출물은 행마다 문자 하나이므로 이 방식의 출력이 그대로 옮겨진다.

---

## 3. 전체 결과

| 모델 | 정확도 | 95% 신뢰구간 | 정답수 | s/장 | test 5,074장 추정 |
|---|---|---|---|---|---|
| **Qwen3-VL-4B-Instruct** | **86.3%** | 81.9 – 89.7% | 258/299 | 0.11 | **10분** |
| Kanana-1.5-V-3B-Instruct | 80.3% | 75.4 – 84.4% | 240/299 | 0.35 | 29분 |
| Qwen2.5-VL-3B-Instruct *(베이스라인)* | 77.3% | 72.2 – 81.6% | 231/299 | 0.13 | 11분 |
| 텍스트 전용 기준선 | 57.5% | 51.9 – 63.0% | 172/299 | — | — |
| 랜덤 | 25.0% | — | — | — | — |

### 3.1 텍스트 전용 기준선이 왜 중요한가

이미지를 **전혀 주지 않고** 질문+선택지 텍스트만으로 학습한 분류기(문자 n-gram TF-IDF + 로지스틱 회귀)가 **57.5%**를 낸다. 데이터에 강한 언어적 사전확률이 박혀 있기 때문이다 — 재질 문제 정답의 약 28%가 플라스틱 계열이고, 개수 문제 정답 숫자가 "2개"인 경우가 44%다.

따라서 **VLM이 57.5%를 넘지 못하면 이미지를 보고 있지 않다는 뜻**이다. 세 모델 모두 이 선을 크게 넘었다(+19.7 ~ +28.8%p). 이미지가 실제로 기여하고 있음이 확인됐다.

### 3.2 순위의 통계적 유의성 (McNemar 검정)

같은 문항을 푼 결과이므로 짝지은 검정을 썼다.

| 비교 | A만 정답 | B만 정답 | 차이 | p값 | 판정 |
|---|---|---|---|---|---|
| Qwen3 vs Kanana | 39 | 21 | +6.0%p | 0.028 | **유의** |
| Qwen3 vs Qwen2.5 | 38 | 11 | +9.0%p | 0.0002 | **유의** |
| Kanana vs Qwen2.5 | 36 | 27 | +3.0%p | 0.314 | 무의미 |

Qwen3의 1위는 통계적으로 확실하다. Kanana와 Qwen2.5 사이의 3%p는 우연과 구분되지 않는다.

---

## 4. 질문 유형별 분석

| 유형 | n | test 비중 | Qwen3 | Kanana | Qwen2.5 | 텍스트 전용 |
|---|---|---|---|---|---|---|
| 개수 (몇 개입니까) | 99 | 33.9% | **73.7%** | 66.7% | 67.7% | 49.5% |
| 재질 (재질은 무엇) | 96 | 30.1% | 92.7% | 88.5% | 75.0% | 62.5% |
| 색상 (어느 색) | 21 | 7.2% | 95.2% | **100.0%** | 95.2% | 52.4% |
| 기타 (무엇인가요 — 물체 식별) | 83 | 28.9% | 91.6% | 81.9% | 86.7% | 62.7% |

> "기타"는 사실상 물체 식별 문항이다. 실제 예: *"사진에 보이는 재활용품은 무엇인가요?"*, *"사진 속 손에 들린 재활용품은 어떤 종류의 용기인가요?"*

### 4.1 개수 세기가 유일한 약점이다

Qwen3는 재질 93% · 색상 95% · 기타 92%로 고르게 높은데 **개수만 74%**다. 텍스트 전용 기준선 대비 향상폭도 개수가 가장 작다(+24.2%p vs 재질 +30.2%p).

이건 세 모델 공통이다(74% / 67% / 68%). 모델의 문제가 아니라 **과제의 성질과 입력 해상도의 문제**로 보는 것이 타당하다. 원본 이미지는 720×960인데 384×384(=147k 픽셀)로 눌러 넣고 있고, 작은 물체를 세는 일은 여기서 가장 먼저 무너진다.

**개수 문제는 test의 33.9%를 차지한다.** 개수만 74% → 85%로 올려도 전체는 약 +3.7%p 오른다. 남은 개선 여지가 사실상 여기에 몰려 있다.

---

## 5. "한국어 특화 모델이 유리할 것"이라는 가설의 검증

### 5.1 가설과 결과

사전 예상은 이랬다. 질문과 선택지가 전부 한국어이므로, 한국어 이미지 벤치마크에서 앞서는 kanana(K-MMBench 등에서 Qwen2.5-VL-3B 대비 평균 68.3 vs 60.6)가 유리할 것이다.

**결과는 그렇지 않았다.** kanana는 80.3%로 Qwen3(86.3%)에 유의하게 뒤졌다.

### 5.2 격차가 어디서 벌어졌는가

| 유형 | Qwen3 − Kanana |
|---|---|
| 색상 (가장 단순한 지각) | **−4.8%p** (kanana 우위) |
| 재질 | +4.2%p |
| 개수 (시각적으로 가장 어려움) | **+7.1%p** |
| 기타 (물체 식별) | **+9.6%p** |

**격차가 벌어지는 지점이 시각적 난이도를 따라간다.** 색상처럼 단순한 속성 판별에서는 kanana가 오히려 100%로 앞선다. 물체를 식별하고 개수를 세는 쪽으로 갈수록 격차가 벌어진다. 한국어 실력이 결정 변수였다면 유형에 관계없이 고르게 이겼어야 하는데, 그런 패턴이 아니다.

### 5.3 왜 한국어 특화가 이득을 못 냈는가

**이 과제의 언어 부하가 작다.**

- 질문 평균 길이 **32자**, 선택지는 2~4단어("플라스틱", "금속 캔", "3개")
- 이미지 없이 텍스트만으로 이미 **57.5%**가 나온다 → 문제를 *읽는* 것은 세 모델 모두에게 어렵지 않다
- kanana의 한국어 우위를 만든 벤치마크는 한국어 OCR과 긴 한국어 지시 따르기를 측정한다. 여기서는 **둘 다 쓰이지 않는다**

즉 승부는 텍스트가 줄 수 없는 것 — 이미지를 보는 능력 — 에서 갈렸고, 한국어 특화는 그 부분에 기여하지 않았다.

### 5.4 다만 한 가지 반전이 있다 — kanana의 위치 편향

정답 위치별로 쪼개면 kanana의 문제가 언어도 시각도 아닌 곳에 있다.

**정답 위치별 정확도**

| gold 위치 | n | Qwen3 | Kanana | Qwen2.5 |
|---|---|---|---|---|
| (a) | 83 | 92% | 86% | 82% |
| (b) | 73 | 81% | 92% | 75% |
| (c) | 63 | 89% | 95% | 83% |
| (d) | 80 | 84% | **52%** | 70% |

**예측 분포 (정답 위치는 균등하므로 25%가 이상적)**

| 모델 | (a) | (b) | (c) | (d) | 최대 편차 |
|---|---|---|---|---|---|
| Qwen3 | 29.8% | 23.7% | 21.4% | 25.1% | 0.048 |
| Kanana | 27.8% | 31.4% | 26.1% | **14.7%** | **0.103** |
| Qwen2.5 | 29.1% | 24.1% | 24.7% | 22.1% | 0.041 |

kanana는 (d)를 거의 고르지 않는다. **정답이 (d)일 때만 52%로 무너진다** — a/b/c 평균이 90.4%인데도.

**보정 실험.** 예측 분포를 균등에 맞추는 무지도 보정(라벨을 쓰지 않으므로 test에도 적용 가능)을 걸어봤다.

| 모델 | 원본 | 보정 후 | 변화 |
|---|---|---|---|
| Qwen3 | 86.3% | 84.3% | **−2.0%p** |
| Kanana | 80.3% | 84.3% | **+4.0%p** |
| Qwen2.5 | 77.3% | 76.6% | −0.7%p |

kanana는 4%p 회복해 Qwen3와 **동률(84.3%)**이 된다. Qwen3는 이미 잘 보정돼 있어 오히려 손해를 본다.

**해석.** kanana의 6%p 열세 중 상당 부분은 능력 차이가 아니라 위치 편향 아티팩트였다. 산술적 상한(d가 a/b/c 평균만큼 나왔다면)은 90.4%지만, 실제 보정으로 회복된 건 84.3%다 — 편향을 완벽히 제거할 수는 없다는 뜻이다.

**실무적 결론은 그대로다.** Qwen3는 손볼 것 없이 최고 성능이고 3배 빠르다. 다만 *"Qwen3가 kanana보다 6%p 우월하다"*는 서술은 과장이며, **"kanana는 보정이 필요하고 Qwen3는 필요 없다"**가 더 정확하다.

> ⚠️ **Qwen3에는 이 보정을 걸지 말 것.** 2%p 손해다.

---

## 6. 앙상블 · 블렌딩 검증 — 효과 없음

### 6.1 상보성은 존재한다

| 지표 | 값 |
|---|---|
| 셋 중 하나라도 정답 (오라클 상한) | 94.3% |
| 셋 다 정답 | 65.6% |
| 셋 다 오답 | 5.7% |
| Qwen3만 단독 정답 | 15문항 (5.0%) |
| Kanana만 단독 정답 | 13문항 (4.3%) |
| Qwen2.5만 단독 정답 | 3문항 (1.0%) |

오라클 94.3%는 Qwen3 단독 86.3%보다 8%p 높다. 이론적 여지는 있다.

### 6.2 그런데 단순 평균으로는 뽑아내지 못한다

| 조합 | 정확도 |
|---|---|
| Qwen3 단독 | **86.3%** |
| Qwen3 + Kanana | 85.3% |
| Qwen3 + Qwen2.5 | 85.6% |
| 3개 전부 | 86.0% |
| 보정 후 3개 전부 | 84.9% |

**모든 앙상블이 단일 모델보다 낮다.** 약한 모델이 강한 모델의 정답을 끌어내리는 쪽이 더 크다.

### 6.3 텍스트 사전확률 블렌딩도 무효

`w × VLM확률 + (1−w) × 텍스트확률`로 가중치를 탐색했다.

| w (VLM 비중) | 0.00 | 0.50 | 0.70 | 0.80 | 0.90 | **1.00** |
|---|---|---|---|---|---|---|
| 정확도 | 57.5% | 85.6% | 86.0% | 86.0% | 86.0% | **86.3%** |

**최적 가중치가 w = 1.0** — 즉 텍스트 신호를 아예 섞지 않는 것이 최선이다.

### 6.4 왜 그런가

Qwen3가 텍스트 사전확률을 이미 흡수했기 때문이다. 텍스트 분류기가 **틀린** 127문항에서도 Qwen3는 85.0%를 맞힌다(텍스트가 맞힌 문항에서는 87.2%). 즉 Qwen3의 성능은 언어 사전확률에 기대고 있지 않으며, 텍스트 신호를 따로 더해줄 여지가 없다.

| 모델 | 텍스트가 맞힌 문항 (n=172) | 텍스트가 틀린 문항 (n=127) |
|---|---|---|
| Qwen3 | 87.2% | 85.0% |
| Kanana | 81.4% | 78.7% |
| Qwen2.5 | 75.0% | 80.3% |

**→ 앙상블과 블렌딩은 계획에서 제외한다.** 하루 예산에서 이 레버에 시간을 쓸 이유가 없다.

---

## 7. 결론 및 다음 단계

### 7.1 확정 사항

- **모델: `Qwen/Qwen3-VL-4B-Instruct`** (Apache 2.0)
- 추론: a/b/c/d 로짓 비교 유지 (`padding_side="left"` 필수)
- 예측 분포 보정: **걸지 않는다**
- 앙상블·텍스트 블렌딩: **하지 않는다**

### 7.2 남은 개선 레버 (우선순위)

| # | 레버 | 근거 | 비용 |
|---|---|---|---|
| 1 | **입력 해상도 384 → 512/640** | 개수 문제만 74%(나머지 92~95%), test의 33.9% 차지 | 추론 10분 → 최대 27분. 충분히 감당 |
| 2 | LoRA 파인튜닝 (train 5,073장) | 가장 정직한 개선. 추론이 10분이라 예산 대부분을 학습에 쓸 수 있음 | 수 시간 |
| 3 | dev.csv를 개수 문제 보조 학습에 투입 | 개수가 유일한 약점, dev는 개수 71.5% | 중간. 라벨 노이즈 주의 |
| ✕ | 선택지 순서 셔플 TTA | Qwen3는 이미 균형적(편차 0.048) — 기대 낮음 | 추론 N배 |
| ✕ | 앙상블 / 텍스트 블렌딩 | 실측으로 무효 확인 (6절) | — |

### 7.3 권장 순서

1. **해상도 실험 먼저** (384 / 512 / 640, val 299장) — 약 30분. 개수 문제 정확도가 목표 지표.
2. 그 결과로 학습 해상도를 확정한 뒤 LoRA 파인튜닝.
3. 학습 전 zero-shot 제출을 한 번 확보해두면 안전망이 된다(86.3%는 이미 쓸 만한 점수다).

---

## 부록 A. 재현 방법

```bash
# 3모델 일괄 (노트북)
step1_2_bakeoff.ipynb          # 로컬
step1_2_bakeoff_colab.ipynb    # Colab

# kanana 단독 (스크립트)
python kanana_bakeoff.py --dry-run   # 경로·split·기준선 점검
python kanana_bakeoff.py --smoke     # 8장 빠른 점검
python kanana_bakeoff.py             # 본 측정
```

산출물은 `bakeoff_out/`에 저장된다 — `bakeoff_results.json`, `results_kanana_v_3b.json`, `text_only_probs.npy`, `bakeoff.png`.

## 부록 B. 모델 로딩 시 겪은 문제와 해결

kanana는 remote code 모델이라 세 번 깨졌다. 다른 remote code 모델을 붙일 때도 같은 순서로 만나게 된다.

| 증상 | 원인 | 해결 |
|---|---|---|
| `Unrecognized configuration class ... AutoModelForImageTextToText` | kanana는 `auto_map`에 `AutoModelForVision2Seq`로만 등록됨. transformers 5.x에는 그 클래스가 아예 없음 | `config.auto_map`을 읽어 `get_class_from_dynamic_module`로 실제 클래스를 직접 로드 (Auto 등록 우회) |
| `No module named 'timm'` | vision encoder가 timm 기반 | 에러 메시지에서 패키지명을 파싱해 자동 설치 후 재시도. 재시도 전 `sys.modules`의 `transformers_modules*` 제거 |
| `FlashAttention2 ... flash_attn not installed` | modeling.py에 try/except가 있는데도 터짐. **1차 flash 호출이 실패하면서 config를 오염시키고**, 폴백 호출이 그 config를 그대로 써서 다시 터짐 | `PreTrainedModel._from_config` 등을 컨텍스트 매니저로 잠시 몽키패치해 flash 요청을 sdpa/eager로 치환 |

> **`flash_attn` 설치는 해결책이 아니다.** FlashAttention-2는 Ampere(sm80) 이상 전용이라 T4(sm75)에서는 애초에 동작하지 않고, 빌드에만 10분 이상 걸린다.

그 밖의 실전 주의사항:

- **`padding_side="left"` 필수** — 로짓 스코어링은 마지막 토큰 위치를 읽는데, 오른쪽 패딩이면 그 자리가 패딩 토큰이라 정확도가 무너진다.
- **모델을 바꿀 때마다 a/b/c/d 토큰 id 4개가 서로 다른지 확인** — 겹치면 정확도가 조용히 25% 근처에 고정된다.
- **sm80 미만 GPU는 bfloat16 미지원** — `get_device_capability()[0] >= 8`로 fp16/bf16 자동 선택.

## 부록 C. 원자료

- 정답 위치 분포 (val 299장): (a) 83 · (b) 73 · (c) 63 · (d) 80
- 측정 환경: NVIDIA GeForce RTX 5060 Ti, bfloat16, 양자화 없음
- 텍스트 전용 기준선: 문자 n-gram TF-IDF(2–4gram) + 로지스틱 회귀(C=2.0), fit 4,566문항으로 학습

---

*본 보고서의 모든 수치는 `bakeoff_out/`의 저장된 확률값에서 직접 재계산했다. 앙상블·보정·상보성 분석은 사후 분석이며 추가 GPU 실행 없이 수행했다.*
